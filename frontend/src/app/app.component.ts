import {Component} from '@angular/core';

@Component({
  selector: 'app-root',
  template: `
    <body class="mat-app-background">
    <app-form></app-form>
    </body>
  `
})
export class AppComponent {
  title = 'frontend-app';
}
